Description: Removed hard dependency on Python 3.5
 When a system has both Python 3.5 and a newer version installed,
 cmake will only pick up 3.5.
 This causes problems with SIP modules, which are only installed
 for a single Python version.
Author: Gregor Riepl <onitake@gmail.com>
Bug-Ultimaker: https://github.com/Ultimaker/Cura/issues/2715
Last-Update: 2017-12-18
---

--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -34,7 +34,10 @@
     CREATE_TRANSLATION_TARGETS()
 endif()
 
-find_package(PythonInterp 3.5.0 REQUIRED)
+find_package(PythonInterp 3 REQUIRED)
+if (PYTHON_VERSION_MAJOR LESS 3 OR PYTHON_VERSION_MAJOR EQUAL 3 AND PYTHON_VERSION_MINOR LESS 5)
+    message(FATAL_ERROR "At least Python 3.5 is required")
+endif()
 
 install(DIRECTORY resources
         DESTINATION ${CMAKE_INSTALL_DATADIR}/cura
--- a/cmake/CuraTests.cmake
+++ b/cmake/CuraTests.cmake
@@ -4,7 +4,10 @@
 enable_testing()
 include(CMakeParseArguments)
 
-find_package(PythonInterp 3.5.0 REQUIRED)
+find_package(PythonInterp 3 REQUIRED)
+if (PYTHON_VERSION_MAJOR LESS 3 OR PYTHON_VERSION_MAJOR EQUAL 3 AND PYTHON_VERSION_MINOR LESS 5)
+    message(FATAL_ERROR "At least Python 3.5 is required")
+endif()
 
 function(cura_add_test)
     set(_single_args NAME DIRECTORY PYTHONPATH)
